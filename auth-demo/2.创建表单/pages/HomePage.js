import React from 'react';
import Home from '../components/Home';

/**
 * 所有需要在主页中显示的都配置在这里统一映射
 */
const HomePage = () => {
    return (
        <div>
            <Home />
        </div>
    );
};

export default HomePage;